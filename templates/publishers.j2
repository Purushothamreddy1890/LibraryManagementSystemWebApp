<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="urf-8">
        <title>Project Step 3 draft</title>
        <link rel="stylesheet" type= "text/css" href="{{ url_for('static', filename='app.css') }}">
    </head>
    <body>
        <header>
            <div class ="container">
                <nav>
                    <ul class = "main">
                    <li><a href="index.html">Home</a></li>
                    <li><a href="students.html">Students</a></li>
                    <li><a href="authors.html">Authors</a></li>
                    <li><a href="publishers.html">Publishers</a></li>
                    <li><a href="books.html">Books</a></li>
                    <li><a href="borrowers.html">Borrow Reports</a></li>
                    </ul>
                </nav>
            </div>
        </header>
        
<h1>Publishers</h1>
<p>Browse Publishers Database</p>
<table>
    <thead>
        <tr>
            <th>ID</th>
            <th>Publisher Name</th>
            <th>Publisher Email</th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
    </thead>
    <tbody>
        {% for row in results%}
        <tr>
            <td>{{row.publisher_id}}</td>
            <td>{{row.publisher_name}}</td>
            <td>{{row.publisher_email}}</td>
            <td><button onclick="location.href='/update_publisher/{{row.publisher_id}}'" type="button">Edit</button></td>
            <td><button onclick="location.href= '/delete_publisher/{{row.publisher_id}}'" type="button">Delete</button></td>
        </tr> 
        {% endfor %}
    </tbody>
</table>

    <br>
<div id="insert"> <!-- insert -->
    <form method="POST" id="insert">
        <legend><strong>Add Publishers </strong></legend>
          <fieldset class="fields">
            <label> Publisher Name</label> 
            <input type="text" name="publisherName">
            <label> Publisher Email </label> 
            <input type="text" name="publisherEmail">
      </fieldset>
          <input class="button" type="submit" name="add" value="Add Publishers">
	</form> 
</div>
    </body>
</html>